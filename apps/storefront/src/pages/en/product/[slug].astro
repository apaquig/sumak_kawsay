---
import ProductPage from '../../../components/ProductPage.astro';
import type { Product } from '../../../data/products';
import { getPublishedProducts } from '../../../lib/catalog';
import BaseLayout from '../../../layouts/BaseLayout.astro';

export async function getStaticPaths() {
  const products = await getPublishedProducts('en');
  return products.map((product) => ({
    params: { slug: product.slug },
    props: { product },
  }));
}

interface Props {
  product: Product;
}

const { product } = Astro.props;
const content = product.translations.en;
const productSchema = {
  '@context': 'https://schema.org',
  '@type': 'Product',
  name: content.name,
  description: content.description,
  image: new URL(product.image.url, Astro.site),
  sku: product.id,
  category: product.category.label.en,
  material: content.materials,
  countryOfOrigin: 'EC',
};
const breadcrumbSchema = {
  '@context': 'https://schema.org',
  '@type': 'BreadcrumbList',
  itemListElement: [
    { '@type': 'ListItem', position: 1, name: 'Home', item: new URL('/en/', Astro.site) },
    { '@type': 'ListItem', position: 2, name: 'Collection', item: new URL('/en/shop/', Astro.site) },
    { '@type': 'ListItem', position: 3, name: content.name },
  ],
};
---

<BaseLayout lang="en" title={content.seoTitle} description={content.seoDescription} canonicalPath={`/en/product/${product.slug}/`} alternateEs={`/es/producto/${product.slug}/`} alternateEn={`/en/product/${product.slug}/`} image={product.image.url} schema={[productSchema, breadcrumbSchema]}>
  <ProductPage lang="en" product={product} />
</BaseLayout>

